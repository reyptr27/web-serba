import { Jost, Inter } from "next/font/google";

export const jost = Jost({ subsets: ["latin"] });
export const inter = Inter({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
  style: ["normal"],
});
